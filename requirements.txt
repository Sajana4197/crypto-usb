PySide6>=6.7,<7.0
pycryptodome>=3.20
cryptography>=42.0
psutil>=5.9
pywin32>=306; sys_platform == "win32"

pytest>=8.0

# Optional, per architecture spec: encrypted SQLite database.
# Uncomment if SQLCipher-backed storage is required for a later phase.
# sqlcipher3-binary>=0.5
