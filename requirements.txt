PySide6>=6.7,<7.0
pycryptodome>=3.20
cryptography>=42.0
psutil>=5.9
pywin32>=306; sys_platform == "win32"

pytest>=8.0

# Encrypted SQLite database (Phase 23), per architecture spec.
# `sqlcipher3-binary` (the package originally earmarked here) ships only
# manylinux wheels on PyPI — no Windows build at all — which is a
# non-starter for this project's Windows-only .exe target. `sqlcipher3`
# (no `-binary` suffix) is the same upstream project and, as of 0.6,
# bundles the same statically-linked SQLCipher binary but also publishes
# win_amd64 wheels; the importable module is still `sqlcipher3.dbapi2`
# either way.
sqlcipher3>=0.6
